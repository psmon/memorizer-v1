This project uses dotnet 9.0 and utilizes LLM.
CreateGraphQueryPrompt and CreateRelationshipSuggestionPrompt in src/Memorizer/Prompts/PromptTemplates.cs have never been used,
and we want to manage and improve LLM instruction prompts in the code at a single point.
Refer to the following instructions and information.

# Improvement Instructions 
- Consolidate fragmented prompt usage into PromptTemplates
- For duplicate prompts with the same purpose but differences, analyze both contents and integrate them into an improved prompt
- Keep used prompts in English as before

## Project Location
- Located in the src/Memorizer/ subdirectory.

## Local Testing Method
- Can be tested locally through docker-compose.local-psmon.yml
- This application is built and runs through memorizer. When rebuild/restart is needed, please use only Docker without dotnet CLI
- After running Docker, when testing through API, for authentication, check the authentication method and use admin/admin123
- During testing improvements, database schema changes are not allowed, and postgres schema understanding and read-only access is utilized. Postgres doesn't need to be restarted and only queries will be performed after improvements.

## Documentation After Improvement
- After build and testing are complete, document the improved prompts and concepts, including the location where prompts are organized, in PROMPT-REVIEW.md